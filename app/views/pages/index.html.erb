<h1>home page</h1>
<ul>
  <% @products.each do |product| %>
    <li>
      <h4><%= link_to product.name , product_path(product.id) %></h4>
      <p><%= product.price %></p>
    </li>
  <% end %>
</ul>
